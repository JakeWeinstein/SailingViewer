import { describe, test } from 'vitest'

// youtube-import API route is created in Plan 07.
// These stubs will be filled in once the route exists.

describe('POST /api/youtube/import', () => {
  test.todo('returns 403 for non-captain')
  test.todo('imports videos from uploads playlist')
  test.todo('skips already-imported videos')
})
