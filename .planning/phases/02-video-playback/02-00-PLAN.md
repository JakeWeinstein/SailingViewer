---
phase: 02-video-playback
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - test/setup.ts
  - lib/types.test.ts
  - lib/youtube-oauth.test.ts
  - components/VideoWatchView.test.tsx
  - __tests__/api/youtube-import.test.ts
  - __tests__/api/sessions.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - VID-01
  - VID-02
  - VID-03
  - VID-04
  - VID-05

must_haves:
  truths:
    - "Vitest runs and exits cleanly with zero tests failing"
    - "Test stubs exist for every module the VALIDATION.md requires"
    - "Window.YT mock is available in test environment for component tests"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest framework config with React plugin and path aliases"
      contains: "plugin-react"
    - path: "test/setup.ts"
      provides: "Global test setup: mock next/navigation, global fetch, window.YT"
      contains: "window.YT"
    - path: "lib/types.test.ts"
      provides: "Unit tests for youtubeEmbedUrl, extractYouTubeInfo, formatTime, parseTimestamp"
    - path: "lib/youtube-oauth.test.ts"
      provides: "Unit test stubs for OAuth helpers"
    - path: "components/VideoWatchView.test.tsx"
      provides: "Smoke render + chapter interaction + auto-advance callback test stubs"
    - path: "__tests__/api/youtube-import.test.ts"
      provides: "Auth guard + mock googleapis response test stubs"
    - path: "__tests__/api/sessions.test.ts"
      provides: "Session video API test stubs checking youtube_video_id field"
  key_links:
    - from: "vitest.config.ts"
      to: "test/setup.ts"
      via: "setupFiles config"
      pattern: "setupFiles.*test/setup"
---

<objective>
Install Vitest and create the test infrastructure and all test file stubs required by VALIDATION.md Wave 0.

Purpose: The validation gate requires Vitest installed, configured, and all test stubs in place before any implementation plans run. This ensures every subsequent plan can run `npx vitest run` for feedback.
Output: Vitest config, test setup, 5 test stub files, all passing (stubs use `test.todo` or trivial assertions).
</objective>

<execution_context>
@/Users/jakeweinstein/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakeweinstein/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-video-playback/02-VALIDATION.md

@lib/types.ts
@package.json
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest + create config + test setup</name>
  <files>package.json, vitest.config.ts, test/setup.ts</files>
  <action>
1. Run `npm install --save-dev vitest @vitest/ui @testing-library/react @testing-library/jest-dom @vitejs/plugin-react jsdom`

2. Create `vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import react from '@vitejs/plugin-react'
   import path from 'path'

   export default defineConfig({
     plugins: [react()],
     test: {
       environment: 'jsdom',
       setupFiles: ['./test/setup.ts'],
       globals: true,
       alias: {
         '@': path.resolve(__dirname, '.'),
       },
     },
   })
   ```

3. Create `test/setup.ts` with:
   - `import '@testing-library/jest-dom'`
   - Mock `next/navigation` (useRouter, useSearchParams, usePathname)
   - Mock global `fetch` as `vi.fn()`
   - Mock `window.YT` with a minimal Player constructor stub:
     ```typescript
     const mockPlayer = {
       getCurrentTime: vi.fn(() => 0),
       seekTo: vi.fn(),
       loadVideoById: vi.fn(),
       destroy: vi.fn(),
       getPlayerState: vi.fn(() => 1),
     }
     window.YT = {
       Player: vi.fn(() => mockPlayer),
       PlayerState: { ENDED: 0, PLAYING: 1, PAUSED: 2, BUFFERING: 3, CUED: 5 },
     } as any
     ```
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/Things/TheoryForm && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Vitest installed, config created with React plugin and jsdom, test setup mocks next/navigation + fetch + window.YT</done>
</task>

<task type="auto">
  <name>Task 2: Create all test file stubs from VALIDATION.md</name>
  <files>lib/types.test.ts, lib/youtube-oauth.test.ts, components/VideoWatchView.test.tsx, __tests__/api/youtube-import.test.ts, __tests__/api/sessions.test.ts</files>
  <action>
1. Create `lib/types.test.ts` with real tests for existing pure functions (these can be written now since the functions already exist):
   - `describe('youtubeEmbedUrl')` — test that it returns correct embed URL format
   - `describe('youtubeThumbnailUrl')` — test correct thumbnail URL format (if function exists)
   - `describe('extractYouTubeInfo')` — test YouTube URL parsing (standard, short, with timestamp)
   - `describe('formatTime')` — test seconds to MM:SS formatting
   - `describe('parseTimestamp')` — test MM:SS string to seconds parsing
   Import these functions from `@/lib/types` (or `../lib/types` depending on alias setup).

2. Create `lib/youtube-oauth.test.ts` with `test.todo` stubs:
   - `test.todo('getStoredTokens returns null when no tokens stored')`
   - `test.todo('storeTokens upserts into app_config')`
   - `test.todo('getAuthenticatedYouTube throws when no tokens')`
   These are stubs because youtube-oauth.ts does not exist yet (Plan 01 creates it).

3. Create `components/VideoWatchView.test.tsx` with `test.todo` stubs:
   - `test.todo('renders video player container')`
   - `test.todo('clicking chapter calls seekTo with correct timestamp')`
   - `test.todo('auto-advance fires when player state is ENDED')`
   These are stubs because VideoWatchView will be rewritten in Plan 03.

4. Create `__tests__/api/youtube-import.test.ts` with `test.todo` stubs:
   - `test.todo('returns 403 for non-captain')`
   - `test.todo('imports videos from uploads playlist')`
   - `test.todo('skips already-imported videos')`

5. Create `__tests__/api/sessions.test.ts` with `test.todo` stubs:
   - `test.todo('GET returns session_videos with youtube_video_id field')`
   - `test.todo('session video list includes position ordering')`

Ensure `__tests__/api/` directory is created before writing files.
  </action>
  <verify>
    <automated>cd /Users/jakeweinstein/Desktop/Things/TheoryForm && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 test files exist. lib/types.test.ts has real passing tests for existing pure functions. Remaining 4 files have test.todo stubs. `npx vitest run` exits cleanly with no failures.</done>
</task>

</tasks>

<verification>
- `npx vitest run` exits with 0 failures
- All 5 test files exist at the paths specified in VALIDATION.md
- `vitest.config.ts` exists with React plugin and jsdom environment
- `test/setup.ts` mocks next/navigation, fetch, and window.YT
- `lib/types.test.ts` has real passing assertions (not just stubs)
</verification>

<success_criteria>
Vitest is installed and configured. Running `npx vitest run` completes cleanly. All test file stubs required by VALIDATION.md exist. The lib/types.test.ts file has real passing tests for existing pure utility functions. The validation gate Wave 0 checklist is fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-video-playback/02-00-SUMMARY.md`
</output>
