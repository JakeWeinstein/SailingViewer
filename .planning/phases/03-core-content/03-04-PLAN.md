---
phase: 03-core-content
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-03]
files_modified:
  - components/VideoWatchView.tsx
  - components/VideoWatchView.test.tsx
  - components/DashboardView.tsx
  - app/page.tsx
autonomous: false
requirements: [COMM-01, COMM-02, COMM-03, CONT-01]

must_haves:
  truths:
    - "Timestamp auto-fills from YouTube player into comment composer"
    - "Clicking a comment timestamp badge seeks the player to that moment"
    - "Replies collapse under parent with count badge; tap to expand"
    - "Users can edit and delete their own comments with edited indicator"
    - "New comments appear via 30-second polling without disrupting scroll"
    - "Flag for captain review is a checkbox in the comment composer"
    - "Comments sorted chronologically oldest first"
    - "Session browser shows current session prominently with past sessions in list"
    - "Each video in session list shows comment count and flagged count"
  artifacts:
    - path: "components/VideoWatchView.tsx"
      provides: "Comment UI with polling, edit/delete, threading, timestamp seek"
      min_lines: 200
    - path: "components/VideoWatchView.test.tsx"
      provides: "RTL tests for timestamp capture, seekTo, reply toggle, comment stats"
    - path: "app/page.tsx"
      provides: "Session browser with current + past sessions, video stats"
  key_links:
    - from: "components/VideoWatchView.tsx"
      to: "/api/comments"
      via: "fetch for CRUD + 30s polling interval"
      pattern: "api/comments"
    - from: "components/VideoWatchView.tsx"
      to: "YT.Player"
      via: "getCurrentTime() for timestamp + seekTo() for timestamp click"
      pattern: "getCurrentTime|seekTo"
    - from: "app/page.tsx"
      to: "/api/sessions"
      via: "fetch sessions with video stats"
      pattern: "api/sessions"
---

<objective>
Wire the comment UI (polling, threading, edit/delete, timestamp interaction) into VideoWatchView and build the session browser UI showing current + past sessions with comment stats.

Purpose: This is the user-facing layer — where team members interact with comments on practice videos and browse weekly sessions. All the API work from Plans 01 and 03 gets surfaced here.

Output: Fully functional comment experience in VideoWatchView, session browser in the home page, RTL test suite for comment UI behaviors.
</objective>

<execution_context>
@/Users/jakeweinstein/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakeweinstein/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-content/03-CONTEXT.md
@.planning/phases/03-core-content/03-RESEARCH.md
@.planning/phases/03-core-content/03-01-SUMMARY.md
@.planning/phases/03-core-content/03-03-SUMMARY.md

<interfaces>
<!-- Comment API contracts from Plan 01 -->
```
POST /api/comments — auth required, body: { video_id, session_id, timestamp_seconds, comment_text, send_to_captain, parent_id }
GET /api/comments?videoId=X — returns comments with reply_count, sorted oldest first
GET /api/comments?parentId=X — returns replies for parent
PATCH /api/comments/[id] — auth required, body: { comment_text }, sets is_edited
DELETE /api/comments/[id] — auth required, ownership check
```

<!-- Session API contracts from Plan 03 -->
```
GET /api/sessions — returns sessions with session_videos, each video has comment_count + flagged_count
POST /api/sessions/[id] { action: 'close' } — captain close + carry forward
POST /api/sessions/[id] { action: 'add-video', youtube_url } — add video to session
```

<!-- YouTube player interface (already in VideoWatchView) -->
```typescript
// YT.Player instance methods available:
// player.getCurrentTime(): number
// player.seekTo(seconds: number, allowSeekAhead: boolean): void
// player.getPlayerState(): number (1=PLAYING, 2=PAUSED)
```

<!-- Auth context available in components -->
```typescript
// From cookies/JWT: userRole ('captain'|'contributor'|'viewer'), userName, userId
// getTokenPayload available server-side; client components read from props passed by server page
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VideoWatchView comment experience</name>
  <files>components/VideoWatchView.tsx, components/VideoWatchView.test.tsx</files>
  <behavior>
    - Timestamp auto-fill: when comment textarea receives focus and a mock player returns getCurrentTime()=154, the composer shows "2:34" in a timestamp badge
    - Timestamp clear: clicking X on the timestamp badge removes it, timestamp_seconds is null on submit
    - SeekTo on badge click: clicking a comment's timestamp badge calls player.seekTo(timestamp, true)
    - Reply toggle: clicking "N replies" badge fetches replies and renders them nested below parent; clicking again collapses them
    - Reply count: a comment with reply_count=3 renders "3 replies" text
    - Edit flow: clicking edit on own comment shows textarea pre-filled with comment text; saving calls PATCH and shows "edited" indicator
    - Delete flow: clicking delete on own comment and confirming removes the comment from the list
    - Edit/delete visibility: edit and delete buttons only appear for comments where author_id matches userId prop
    - Flag checkbox: "Flag for captain" checkbox in composer sets send_to_captain=true on POST body
    - Comment stats badges: given a video object with comment_count=5 and flagged_count=2, the video card renders "5" and "2" badge texts
  </behavior>
  <action>
**Write tests first** in `components/VideoWatchView.test.tsx`:

1. Mock the YouTube player: create a mock `YT.Player` object with `getCurrentTime: vi.fn(() => 154)`, `seekTo: vi.fn()`, `getPlayerState: vi.fn(() => 1)`. Mock the global `window.YT` and the player ref/callback used by VideoWatchView.

2. Mock fetch globally to handle GET/POST/PATCH/DELETE `/api/comments` calls. Return mock comment data with `{ id, author_id, author_name, timestamp_seconds, comment_text, reply_count, is_edited, send_to_captain }`.

3. Test timestamp auto-fill: render VideoWatchView with mock player, focus the comment textarea, assert a timestamp badge with "2:34" appears.

4. Test timestamp clear: after auto-fill, click the X button on the badge, assert badge is removed.

5. Test seekTo: render a comment with `timestamp_seconds: 90`, click its timestamp badge, assert `player.seekTo(90, true)` was called.

6. Test reply toggle: render a comment with `reply_count: 3`, click the "3 replies" text, assert fetch was called with `?parentId=X`, assert reply comments render. Click again, assert replies hide.

7. Test edit visibility: render two comments, one with `author_id` matching the `userId` prop and one not. Assert edit/delete buttons only appear on the matching comment.

8. Test edit flow: click edit, assert textarea appears pre-filled, change text, save, assert PATCH was called with new text, assert "edited" indicator appears.

9. Test delete flow: click delete, confirm, assert DELETE was called, assert comment removed from DOM.

10. Test flag checkbox: check the "Flag for captain" checkbox, submit a comment, assert POST body includes `send_to_captain: true`.

**Then implement** the comment section rewrite in VideoWatchView.tsx:

**Comment composer:**
1. Auto-fill timestamp from `player.getCurrentTime()` when the comment textarea receives focus — only if player state is PLAYING(1) or PAUSED(2). Display as formatted time (e.g., "2:34") in a removable badge next to the textarea. User can click X to clear or manually edit.
2. "Flag for captain review" checkbox/toggle below the textarea. Maps to `send_to_captain: true` on POST.
3. Submit calls POST `/api/comments` with auth. Include `video_id`, `session_id`, `timestamp_seconds`, `comment_text`, `send_to_captain`. On success, append the new comment to the local list.
4. For replies: clicking "Reply" on a comment opens a nested composer under that comment. Reply composer has same timestamp + flag behavior. POST with `parent_id` set.

**Comment list:**
1. Fetch comments on mount via GET `/api/comments?videoId=X`. Sorted oldest first (API handles sort).
2. Each comment shows: author name, time ago, timestamp badge (if present), comment text, reply count badge, edit/delete buttons (if own comment).
3. Clicking a timestamp badge calls `player.seekTo(seconds, true)` to seek the video.
4. Reply count badge shows e.g., "3 replies". Clicking it fetches GET `/api/comments?parentId=X` and expands replies below the parent. Click again to collapse. Single-level only (no sub-replies).
5. Edit: clicking edit shows an inline textarea pre-filled with comment text. Save calls PATCH `/api/comments/[id]`. On success, update local state, show "edited" indicator.
6. Delete: clicking delete shows confirmation, then calls DELETE `/api/comments/[id]`. On success, remove from local list.

**30-second polling:**
1. While the comment section is visible (mounted), set up a 30-second interval that re-fetches comments.
2. Use the append-only pattern from RESEARCH.md: compute a Set of existing IDs, filter fresh results to only new ones, append without disrupting scroll position.
3. Cleanup interval on unmount. Handle StrictMode double-invocation via cleanup function.
4. Silent failure on network errors — don't show error toasts for polling failures.

**Styling:**
- Comments: `border-b border-gray-100 py-3` for each comment
- Timestamp badge: `bg-blue-100 text-blue-700 px-1.5 py-0.5 rounded text-xs font-mono cursor-pointer hover:bg-blue-200`
- "edited" indicator: `text-gray-400 text-xs italic` next to timestamp
- Flag checkbox: small checkbox with label "Flag for captain"
- Reply toggle: `text-sm text-gray-500 hover:text-gray-700 cursor-pointer`

The component needs to know the current user's ID to show edit/delete buttons. Accept `userId` and `userRole` as props (passed down from the page component).
  </action>
  <verify>
    <automated>npx vitest run components/VideoWatchView.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Comment composer auto-fills timestamp from player. Timestamp click seeks video. Replies expand/collapse under parent. Edit/delete works for own comments. 30s polling appends new comments. Flag checkbox works. All RTL tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Session browser UI with stats</name>
  <files>app/page.tsx, components/DashboardView.tsx</files>
  <action>
Update the home page session browsing experience and dashboard session management:

**Home page (app/page.tsx) — Session browser:**
1. Fetch sessions from GET `/api/sessions` (which now includes per-video comment_count and flagged_count).
2. Display the current active session prominently at top: session label, list of videos with thumbnails.
3. Each video card shows: YouTube thumbnail, title, comment count badge, flagged-for-review count badge (if > 0, shown in amber).
4. Past sessions (closed_at !== null) listed below in reverse chronological order. Each past session is collapsible — click to expand and see its videos.
5. Clicking a video navigates to the video watch view.

**Dashboard (components/DashboardView.tsx) — Session management:**
1. In the session management sidebar, show the active session with a "Close & Start Next Week" button (captain only).
2. On click: show confirmation with auto-generated next label ("Week of [next Monday]") that the captain can edit. On confirm, POST `/api/sessions/[id]` with `action: 'close'` and optional custom `next_label`.
3. Add "Paste YouTube URL" input field below the active session's video list. On submit, POST `/api/sessions/[id]` with `action: 'add-video'` and the URL. Append the new video to the local list on success.
4. Show per-video stats (comment count, flagged count) in the dashboard session view as well.

Note: The must_have truth "Each video in session list shows comment count and flagged count" is verified via the human checkpoint (Task 3 step 8) as this is visual rendering behavior best confirmed visually. The comment stats badges are also covered by the VideoWatchView.test.tsx behavior test "Comment stats badges" which asserts badge rendering given mock data with counts.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Session browser shows active session prominently. Past sessions are browsable. Per-video comment and flagged counts display. Captain can close session and add videos manually.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 3 end-to-end verification</name>
  <files>N/A</files>
  <action>
Human verifies the complete Phase 3 feature set across all four plans: timestamped comments with threading, edit/delete, 30s polling, captain flagging, session lifecycle with carry-forward, reference tags and chapters, and the redesigned article editor with 4 block types and drag-to-reorder.
  </action>
  <verify>Human confirms all 13 verification steps pass</verify>
  <done>
    1. Start the dev server: `npm run dev`
    2. Log in as captain at /login
    3. Navigate to an active session video — verify comment composer auto-fills timestamp when you click the textarea while video is playing
    4. Post a comment with "Flag for captain" checked — verify it appears with a flag indicator
    5. Edit the comment — verify "edited" indicator appears
    6. Reply to the comment — verify reply appears nested below with reply count badge
    7. Click a timestamp badge — verify video seeks to that moment
    8. Go to the home page session browser — verify active session is prominent, each video card shows comment count badge and flagged count badge
    9. In dashboard, click "Close and Start Next Week" — verify new session is created and flagged comments carried forward
    10. Go to Reference Library — add a tag to a video, then filter by that tag
    11. On a reference video, add a chapter inline
    12. Go to Articles — create a new article with all 4 block types (text, video, image, clip) and drag-reorder blocks
    13. Publish the article and view it — verify all blocks render correctly
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run components/VideoWatchView.test.tsx --reporter=verbose` all tests pass
- Comment timestamp auto-capture works with YouTube player
- 30s polling appends new comments
- Session close creates new session
- All 4 article block types render in viewer
</verification>

<success_criteria>
- Team member can leave timestamped comment, click timestamp to seek, reply to comment, edit/delete own comment
- Captain can flag comments for review, close session with carry-forward, add videos manually
- Reference library has tag filter and inline chapter adding
- Article editor supports 4 block types with drag reorder
- Session browser shows current + past sessions with per-video comment stats
- RTL tests verify timestamp capture, seekTo, reply toggle, edit/delete, flag checkbox, and comment stats badges
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-content/03-04-SUMMARY.md`
</output>
