---
phase: 05-presentation-and-search
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - app/api/comments/route.test.ts
  - app/api/comments/[id]/route.test.ts
  - app/api/comments/reorder/route.test.ts
  - app/api/search/route.test.ts
  - components/PresentationMode.test.tsx
  - components/SearchResults.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 6 test stub files exist and can be discovered by vitest"
    - "Running npx vitest run completes without file-not-found errors for any planned test"
  artifacts:
    - path: "app/api/comments/route.test.ts"
      provides: "Extended stubs for is_reviewed filter (REV-01)"
    - path: "app/api/comments/[id]/route.test.ts"
      provides: "Stubs for is_reviewed mark/unmark (REV-03, REV-07)"
    - path: "app/api/comments/reorder/route.test.ts"
      provides: "Stubs for bulk reorder endpoint (REV-05)"
    - path: "app/api/search/route.test.ts"
      provides: "Stubs for full-text search endpoint (CONT-08)"
    - path: "components/PresentationMode.test.tsx"
      provides: "Stubs for presentation mode UI (REV-02, REV-04, REV-06, REV-07)"
    - path: "components/SearchResults.test.tsx"
      provides: "Stubs for search results UI (CONT-08)"
  key_links: []
---

<objective>
Create Wave 0 test stub files so that all downstream plans can reference per-requirement vitest commands in their verify fields. Each stub contains describe/it blocks with `todo` or `skip` markers — enough for vitest to discover them without false failures.

Purpose: Nyquist compliance — every task verify field needs an automated command that targets a real test file.
Output: 6 test stub files (1 extension, 5 new).
</objective>

<execution_context>
@/Users/jakeweinstein/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakeweinstein/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-presentation-and-search/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API route test stubs</name>
  <files>app/api/comments/route.test.ts, app/api/comments/[id]/route.test.ts, app/api/comments/reorder/route.test.ts, app/api/search/route.test.ts</files>
  <action>
**1. Extend `app/api/comments/route.test.ts`:**
- File already exists. Add a new describe block at the end:
```typescript
describe('GET /api/comments — review queue filter (REV-01)', () => {
  it.todo('returns only is_reviewed=false items when captainOnly=true')
  it.todo('returns all items when is_reviewed filter is not applied')
  it.todo('includes sort_order in response for queue ordering')
})
```

**2. Create `app/api/comments/[id]/route.test.ts`:**
```typescript
import { describe, it } from 'vitest'

describe('PATCH /api/comments/[id] — review lifecycle (REV-03)', () => {
  it.todo('marks comment as reviewed with is_reviewed=true, sets reviewed_at')
  it.todo('restores comment with is_reviewed=false, clears reviewed_at')
  it.todo('rejects non-captain with 403')
  it.todo('returns 400 for invalid body')
})

describe('PATCH /api/comments/[id] — inline reply (REV-07)', () => {
  it.todo('creates reply comment with parent_id linking to original')
})
```

**3. Create `app/api/comments/reorder/route.test.ts`:**
```typescript
import { describe, it } from 'vitest'

describe('PATCH /api/comments/reorder (REV-05)', () => {
  it.todo('updates sort_order for multiple comments in one request')
  it.todo('validates session_id ownership')
  it.todo('rejects non-captain with 403')
  it.todo('returns 400 for empty order array')
})
```

**4. Create `app/api/search/route.test.ts`:**
```typescript
import { describe, it } from 'vitest'

describe('GET /api/search (CONT-08)', () => {
  it.todo('returns results from search_all RPC')
  it.todo('validates q parameter is present and non-empty')
  it.todo('respects limit parameter')
  it.todo('returns empty array for no matches')
  it.todo('does not require authentication')
})
```
  </action>
  <verify>
    <automated>npx vitest run app/api/comments/route.test.ts app/api/comments/[id]/route.test.ts app/api/comments/reorder/route.test.ts app/api/search/route.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>4 API test stub files exist, vitest discovers all todo tests without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create component test stubs</name>
  <files>components/PresentationMode.test.tsx, components/SearchResults.test.tsx</files>
  <action>
**1. Create `components/PresentationMode.test.tsx`:**
```typescript
import { describe, it } from 'vitest'

describe('PresentationMode (REV-02)', () => {
  it.todo('renders split-pane layout with queue sidebar and detail pane')
  it.todo('groups items by sailor name')
  it.todo('shows collapsible author sections')
})

describe('PresentationMode — keyboard shortcuts (REV-04)', () => {
  it.todo('ArrowDown selects next item')
  it.todo('ArrowUp selects previous item')
  it.todo('R marks current item as reviewed')
  it.todo('Escape navigates back to dashboard')
  it.todo('ignores shortcuts when input is focused')
})

describe('PresentationMode — reference panel (REV-06)', () => {
  it.todo('opens reference side panel on button click')
  it.todo('closes reference side panel')
})

describe('PresentationMode — reply (REV-07)', () => {
  it.todo('shows inline reply field for active item')
  it.todo('submits reply via POST /api/comments')
})
```

**2. Create `components/SearchResults.test.tsx`:**
```typescript
import { describe, it } from 'vitest'

describe('SearchResults (CONT-08)', () => {
  it.todo('fetches results from /api/search on mount')
  it.todo('groups results by type: Videos, Comments, Articles, Q&A')
  it.todo('shows top 5 results per section with Show more button')
  it.todo('hides empty sections')
  it.todo('shows no results message for empty response')
  it.todo('saves scroll position to sessionStorage before navigation')
  it.todo('restores scroll position from sessionStorage on mount')
})
```
  </action>
  <verify>
    <automated>npx vitest run components/PresentationMode.test.tsx components/SearchResults.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>2 component test stub files exist, vitest discovers all todo tests without errors.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` completes without file-not-found errors
- All 6 test files are discoverable by vitest
- No false failures (all tests are todo/skip)
</verification>

<success_criteria>
- 6 test stub files exist at the paths specified in VALIDATION.md
- vitest run completes cleanly with all tests marked as todo
</success_criteria>

<output>
After completion, create `.planning/phases/05-presentation-and-search/05-00-SUMMARY.md`
</output>
