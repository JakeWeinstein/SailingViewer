---
phase: 05-presentation-and-search
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - app/dashboard/present/page.tsx
  - components/PresentationMode.tsx
autonomous: false
requirements: [REV-01, REV-02, REV-03, REV-04, REV-05, REV-06, REV-07, CONT-08]
must_haves:
  truths:
    - "All presentation mode and search features work end-to-end"
    - "Keyboard shortcuts function correctly in presentation mode"
    - "Drag reorder persists across page reload"
    - "Search results link to correct content"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of all Phase 5 features: presentation mode (queue, reorder, review lifecycle, reference panel, keyboard shortcuts, reply) and search (global bar, results page, deep links, scroll preservation).

Purpose: Confirm all features work correctly before marking Phase 5 complete.
Output: Human verification of the complete feature set.
</objective>

<execution_context>
@/Users/jakeweinstein/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakeweinstein/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-presentation-and-search/05-01-SUMMARY.md
@.planning/phases/05-presentation-and-search/05-02-SUMMARY.md
@.planning/phases/05-presentation-and-search/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification type check and test suite</name>
  <files>app/dashboard/present/page.tsx, components/PresentationMode.tsx</files>
  <action>
Run full type check and test suite to catch any issues before human verification:
1. `npx tsc --noEmit` — must pass with zero errors
2. `npx vitest run --reporter=verbose` — full test suite must pass
3. If any errors, fix them before proceeding to human verification.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>TypeScript compiles cleanly and all vitest tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end human verification of Phase 5</name>
  <action>
Present the verification checklist to the user. The user will manually test all presentation mode and search features in the running dev server.
  </action>
  <verify>Human confirms all 25 verification steps pass.</verify>
  <done>Human types "approved" confirming all features work correctly.</done>
  <what-built>
Complete Phase 5: Presentation Mode and Global Search

**Presentation Mode** (/dashboard/present):
- Split-pane layout: sortable queue sidebar (left) + video player/item detail (right)
- Items grouped by sailor with collapsible sections
- Drag-to-reorder items within queue (persists to database)
- Mark items as reviewed (disappear from active queue)
- Toggle to archived view to see/restore reviewed items
- Reference video side panel (slide-out from right) with folder browser and chapter list
- Keyboard shortcuts: Arrow keys navigate, R marks reviewed, Escape exits
- Inline reply to items (triggers notification)
- URL-addressable current item (?item=uuid)
- "Present" button in dashboard review tab

**Global Search** (/search):
- Search bar in home page and dashboard navigation
- Results grouped by type: Videos, Comments, Articles, Q&A
- "Show more" expansion per section
- Deep-link navigation to content
- Scroll position preserved on back-nav
  </what-built>
  <how-to-verify>
**Start the dev server:** `npm run dev`

**Presentation Mode (log in as captain):**
1. Navigate to /dashboard, select a session with flagged comments in the Review tab
2. Click the "Present" button — should open /dashboard/present
3. Verify items are grouped by sailor name with collapsible sections
4. Click an item — right pane should show item detail with video player (for comments) or Q&A text
5. Try keyboard: ArrowDown to move to next item, ArrowUp for previous
6. Drag an item to reorder — release and reload the page — order should persist
7. Press R on an item — it should disappear from active queue
8. Toggle to "Archived" view — the reviewed item should appear with a Restore button
9. Click Restore — item returns to active queue
10. Click the "Reference" button — side panel should slide in from right
11. Browse folders, select a reference video — it should play in the panel
12. Verify both practice video and reference video can play simultaneously
13. Type a reply in the inline reply field and send — should work
14. Copy the URL (should contain ?item=uuid) — paste in new tab — should land on same item
15. Press Escape — should return to /dashboard

**Global Search (can test as any user or unauthenticated):**
16. On the home page, locate the search bar in the header
17. Type a search term (e.g., a word from a video title or comment) and press Enter
18. Verify /search?q=term loads with results grouped by type
19. Check that each section has appropriate icons and content previews
20. Click "Show more" on a section with >5 results
21. Click a video result — should navigate to the video player
22. Press browser Back — should return to search results at the same scroll position
23. Click an article result — should navigate to /learn/[id]
24. Click a comment result — should open video at the comment's timestamp
25. Try searching while not logged in — should still work
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 25 verification steps pass as described above.
</verification>

<success_criteria>
- Human confirms presentation mode works end-to-end on big screen layout
- Human confirms search works for all content types
- Human confirms keyboard shortcuts are responsive
- Human confirms drag reorder persists
- Human confirms deep links work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-presentation-and-search/05-04-SUMMARY.md`
</output>
