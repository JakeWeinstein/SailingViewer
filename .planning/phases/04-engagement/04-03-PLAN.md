---
phase: 04-engagement
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - components/NotificationBell.tsx
  - components/ProfileEditor.tsx
  - components/VideoWatchView.tsx
  - components/DashboardView.tsx
  - app/page.tsx
  - app/api/users/route.ts
autonomous: true
requirements: [COMM-04, COMM-05, VID-06, AUTH-05]

must_haves:
  truths:
    - "Authenticated users see a bell icon in the top nav with unread count badge"
    - "Authenticated users see a bell icon in the dashboard sidebar"
    - "Clicking the bell opens a dropdown showing recent notifications with previews and deep-links"
    - "Clicking a notification navigates to the relevant comment/post and marks it as read"
    - "Mark all as read button clears the badge count"
    - "Authenticated users see a bookmark icon in the video player that captures current timestamp"
    - "User can view their saved bookmarks in the dashboard profile section"
    - "Clicking a saved bookmark opens the video at the bookmarked timestamp"
    - "Users list is fetched for @mention autocomplete and passed to QATab, VideoWatchView, and ArticleEditor"
  artifacts:
    - path: "components/NotificationBell.tsx"
      provides: "Bell icon + badge + dropdown panel with notification list"
      min_lines: 60
    - path: "components/ProfileEditor.tsx"
      provides: "Extended with bookmarks section listing saved timestamps"
    - path: "components/DashboardView.tsx"
      provides: "NotificationBell rendered in dashboard sidebar/header area, users prop passed to ArticleEditor"
    - path: "app/page.tsx"
      provides: "NotificationBell in header, users fetch for mentions, deep-link param handling, bookmark passing"
  key_links:
    - from: "components/NotificationBell.tsx"
      to: "/api/notifications"
      via: "fetch for count and list"
      pattern: "fetch.*api/notifications"
    - from: "components/ProfileEditor.tsx"
      to: "/api/bookmarks"
      via: "fetch for list and delete"
      pattern: "fetch.*api/bookmarks"
    - from: "app/page.tsx"
      to: "components/NotificationBell.tsx"
      via: "render in header when authUser is set"
      pattern: "NotificationBell"
    - from: "components/DashboardView.tsx"
      to: "components/NotificationBell.tsx"
      via: "render in dashboard sidebar/header for authenticated users"
      pattern: "NotificationBell"
    - from: "app/page.tsx"
      to: "URL query params"
      via: "reads video/session/comment/view/post params on mount for deep-linking"
      pattern: "searchParams|URLSearchParams"
---

<objective>
Build the NotificationBell component, add bookmark button to VideoWatchView, add bookmarks list to ProfileEditor, wire everything into app/page.tsx and DashboardView.tsx with deep-link support and users list fetching for @mention autocomplete.

Purpose: Complete the user-facing engagement loop — users see notifications (on both public pages and dashboard), navigate to mentioned content, bookmark moments, and manage their bookmarks.
Output: NotificationBell component rendered in both header and dashboard sidebar, bookmark UI in player and profile, deep-link navigation from notifications/bookmarks, users list flowing to mention components including ArticleEditor.
</objective>

<execution_context>
@/Users/jakeweinstein/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakeweinstein/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engagement/04-CONTEXT.md
@.planning/phases/04-engagement/04-RESEARCH.md
@.planning/phases/04-engagement/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 API routes -->
GET /api/notifications:
  ?countOnly=true -> { unread: number }
  (no params) -> [{ id, type, source_id, is_read, created_at, preview?, link?, author_name? }]

PATCH /api/notifications:
  { id?: string, markAll?: boolean } -> { success: true }

GET /api/bookmarks -> [{ id, video_id, session_id, timestamp_seconds, video_title, created_at }]
POST /api/bookmarks -> { video_id, session_id?, timestamp_seconds, video_title? } -> 201
DELETE /api/bookmarks/[id] -> 204

<!-- From existing code -->
From app/page.tsx:
```typescript
type MainView = 'sessions' | 'reference' | 'learn' | 'qa'
// authUser state: { role: string; userName?: string } | null | undefined
// header with sticky top-0 z-30
// QATab rendered with userName prop
// VideoWatchView rendered with video, sessionId, userName, userId, userRole props
```

From components/DashboardView.tsx:
```typescript
interface Props {
  initialSessions: Session[]
  userRole: 'captain' | 'contributor' | 'viewer'
  userName: string
  userId: string
}
// Sidebar with navigation buttons (session, reference, upload, articles, team, profile, youtube)
// ArticleEditor rendered when sidebarView === 'articles' && editingArticle !== null
// ArticleEditor receives: article, userName, folders, onSave, onCancel props
```

From components/ProfileEditor.tsx:
```typescript
// Dashboard profile page — has displayName, password update
// Rendered in DashboardView
```

From lib/types.ts:
```typescript
export function formatTime(seconds: number): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationBell component + users list endpoint + app/page.tsx + DashboardView wiring</name>
  <files>components/NotificationBell.tsx, app/api/users/route.ts, app/page.tsx, components/DashboardView.tsx</files>
  <action>
1. Create `components/NotificationBell.tsx` ('use client'):
   - Props: none (self-contained, fetches own data)
   - State: unreadCount (number), isOpen (boolean), notifications (array), loading (boolean)
   - On mount: fetch GET /api/notifications?countOnly=true to set unreadCount
   - When isOpen becomes true: fetch GET /api/notifications for full list, set notifications
   - Poll every 30 seconds ONLY while isOpen (useEffect with interval, cleanup on close)
   - Render: Bell icon (lucide-react Bell) with relative positioning. If unreadCount > 0, show a red badge (absolute -top-1 -right-1, min-w-[18px], bg-red-500 text-white text-xs rounded-full) displaying the count (cap display at 99+). Click toggles isOpen.
   - Dropdown (when isOpen): absolute right-0 top-full mt-2, w-80 max-h-96 overflow-y-auto, bg-white shadow-xl border rounded-xl z-50. Header with "Notifications" title and "Mark all as read" button (only shown if unreadCount > 0). Clicking "Mark all as read" calls PATCH /api/notifications with { markAll: true }, then sets unreadCount to 0 and marks all local notifications as read.
   - Each notification item: flex row with avatar circle (initials + avatarColor from comment-utils using author_name), text preview (truncated), timeAgo timestamp. Unread items have bg-blue-50 left border. Clicking navigates to notification.link (using window.location.href = link — triggers page load which app/page.tsx handles via URL params) and calls PATCH /api/notifications with { id: notification.id }.
   - Empty state: "No notifications yet" centered text with muted color.
   - Click outside to close: useEffect with document click listener, check if click target is outside the bell container ref.

2. Update `app/api/users/route.ts`:
   - Currently captain-only GET. Modify: any authenticated user (getTokenPayload succeeds) can call GET and receive basic fields (id, username, display_name). Captain still gets full details (role, is_active, created_at, is_seed). Check role after the query — for non-captain, map results to only { id, username, display_name: row.display_name }. This enables the @mention user list fetch for all authenticated roles.

3. Update `app/page.tsx`:
   - Import NotificationBell. In the header, after the auth check area (where "Dashboard" link is shown for authUser), add <NotificationBell /> conditionally when authUser is not null.
   - Add state: mentionUsers (Array<{ id: string, username: string, displayName: string }>), initialized to [].
   - When authUser becomes set (not null), fetch GET /api/users. Map response to { id, username, displayName: display_name } format. Set mentionUsers.
   - Pass users={mentionUsers} to QATab component.
   - Pass users={mentionUsers} to VideoWatchView component.
   - Deep-link handling: in a useEffect on mount, read URL search params (window.location.search). Parse: video, session, comment, view, post. If view=qa, set mainView to 'qa'. If video+session are set, set watchTarget to { videoId: video, sessionId: session }. After reading params, call window.history.replaceState({}, '', '/') to clear the URL (prevents re-triggering on refresh). The comment param is informational for now — the video will open and the user can find the comment. The post param for Q&A is also informational — navigating to Q&A tab is sufficient.

4. Update `components/DashboardView.tsx` (per locked decision: bell icon in dashboard sidebar):
   - Import NotificationBell from './NotificationBell'.
   - Render <NotificationBell /> in the dashboard sidebar header area. Place it near the top of the sidebar, before the navigation buttons. The dashboard always has an authenticated user, so no conditional rendering needed — just render the bell.
   - Suggested placement: in the sidebar header area (near the "TheoryForm" or dashboard title at the top of the sidebar), aligned right. Add a small flex row with the bell at the right edge.
   - Also: fetch the users list for @mention autocomplete and pass it to ArticleEditor. Add state: mentionUsers (same pattern as app/page.tsx). Fetch GET /api/users on mount (dashboard is always authenticated). Pass users={mentionUsers} to ArticleEditor as a new prop where ArticleEditor is rendered.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>NotificationBell renders bell icon with unread badge, opens dropdown with notification list, supports mark-as-read and mark-all-read. Bell appears in both app/page.tsx header (public pages) and DashboardView sidebar (dashboard). Users endpoint returns basic user list for all authenticated users. app/page.tsx shows bell for authenticated users, fetches users for @mention, handles deep-link URL params. DashboardView passes users to ArticleEditor for @mention autocomplete.</done>
</task>

<task type="auto">
  <name>Task 2: Bookmark button in player + bookmark list in profile</name>
  <files>components/VideoWatchView.tsx, components/ProfileEditor.tsx</files>
  <action>
1. Update `components/VideoWatchView.tsx`:
   - Add bookmark button to the player controls area (near the existing timestamp/favorite controls). Use Bookmark icon from lucide-react. Only show when userId is present (authenticated).
   - State: bookmarkFlash (boolean) for brief feedback animation.
   - handleBookmark(): guard with if (!userId || !playerRef.current) return. Get timestamp via Math.floor(playerRef.current.getCurrentTime()). Guard with player state check — only capture when getPlayerState() returns 1 (PLAYING) or 2 (PAUSED), otherwise show brief "Play video first" feedback. POST to /api/bookmarks with { video_id: video.id, session_id: sessionId, timestamp_seconds: ts, video_title: video.name }. On 201: set bookmarkFlash true, setTimeout to clear after 1500ms. On 409 (duplicate): show brief "Already bookmarked" feedback. Use same flash pattern.
   - Render bookmark button: <button onClick={handleBookmark} className="..." title="Bookmark this moment"><Bookmark className="h-4 w-4" />{bookmarkFlash && <span className="text-xs text-green-600 ml-1">Saved!</span>}</button>

2. Update `components/ProfileEditor.tsx`:
   - Add a "Bookmarks" section below the existing profile editing section.
   - State: bookmarks (array), loadingBookmarks (boolean).
   - On mount: fetch GET /api/bookmarks, set bookmarks.
   - Render bookmarks list: each bookmark shows video_title (or "Untitled" fallback), formatted timestamp (formatTime from lib/types.ts), and timeAgo created_at. Each item is clickable — clicking navigates to `/?video=${bookmark.video_id}&session=${bookmark.session_id}` (which app/page.tsx deep-link handler picks up). Add a delete button (Trash2 icon) that calls DELETE /api/bookmarks/${bookmark.id}, then removes from local state.
   - Sort bookmarks by created_at descending (most recent first — already returned sorted by API).
   - Empty state: "No bookmarks yet. Use the bookmark icon while watching a video to save a timestamp."
   - Section header: "Saved Bookmarks" with Bookmark icon.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Bookmark button in VideoWatchView captures current timestamp and saves via API with duplicate prevention. ProfileEditor shows saved bookmarks with delete and navigation. Deep-link from bookmark opens the video.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx vitest run --reporter=verbose` passes (existing tests still green)
- NotificationBell renders conditionally for auth users on public pages and always in dashboard
- Bookmark button appears in player controls for auth users
</verification>

<success_criteria>
- Bell icon with red unread badge visible in header for authenticated users on public pages
- Bell icon with red unread badge visible in dashboard sidebar for all dashboard users
- Dropdown opens showing notification list with previews and time-ago
- Clicking notification navigates to deep-linked content
- Mark all as read clears badge count immediately
- Bookmark icon in video player captures current timestamp via YT API
- Player state guard prevents bookmarking at timestamp 0
- Duplicate bookmark returns user-friendly feedback (not error)
- ProfileEditor shows bookmarks list with video title, timestamp, delete
- Clicking bookmark navigates to video at bookmarked timestamp
- Users list fetched once and passed to QATab + VideoWatchView for @mention autocomplete
- Users list fetched in DashboardView and passed to ArticleEditor for @mention autocomplete
- URL params cleared after deep-link handling to prevent re-triggering
</success_criteria>

<output>
After completion, create `.planning/phases/04-engagement/04-03-SUMMARY.md`
</output>
