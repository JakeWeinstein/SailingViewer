---
phase: 04-engagement
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - components/MentionTextarea.tsx
  - components/QATab.tsx
  - components/VideoWatchView.tsx
  - components/ArticleEditor.tsx
  - components/ArticleViewer.tsx
  - lib/comment-utils.ts
autonomous: true
requirements: [QA-01, QA-03, AUTH-05]

must_haves:
  truths:
    - "Typing @ in a comment or Q&A textarea opens a dropdown of team members filtered by input"
    - "Selecting a user from the dropdown inserts @username into the text"
    - "Arrow keys navigate the dropdown, Enter/Tab selects, Escape dismisses"
    - "Q&A post composer has an optional YouTube URL field that previews the embed before posting"
    - "@mentions render as bold blue text in displayed comments, Q&A posts, and article text blocks"
    - "Typing @ in an article text block textarea opens the mention autocomplete dropdown"
  artifacts:
    - path: "components/MentionTextarea.tsx"
      provides: "Reusable textarea with @mention autocomplete dropdown"
      min_lines: 80
    - path: "components/QATab.tsx"
      provides: "Extended Q&A tab with YouTube attachment field and MentionTextarea"
    - path: "components/VideoWatchView.tsx"
      provides: "Comment composer uses MentionTextarea"
    - path: "components/ArticleEditor.tsx"
      provides: "Article text block textarea replaced with MentionTextarea"
    - path: "components/ArticleViewer.tsx"
      provides: "Article text blocks render @mentions as bold blue text"
    - path: "lib/comment-utils.ts"
      provides: "parseMentions re-export or renderMentionText utility"
  key_links:
    - from: "components/MentionTextarea.tsx"
      to: "/api/users"
      via: "fetch users list on mount for autocomplete"
      pattern: "fetch.*api/users"
    - from: "components/QATab.tsx"
      to: "components/MentionTextarea.tsx"
      via: "import MentionTextarea for post and reply composers"
      pattern: "MentionTextarea"
    - from: "components/VideoWatchView.tsx"
      to: "components/MentionTextarea.tsx"
      via: "import MentionTextarea for comment composer"
      pattern: "MentionTextarea"
    - from: "components/ArticleEditor.tsx"
      to: "components/MentionTextarea.tsx"
      via: "import MentionTextarea for text block editing"
      pattern: "MentionTextarea"
    - from: "components/ArticleViewer.tsx"
      to: "lib/mention-utils.ts"
      via: "parseMentions for rendering @mentions in article text blocks"
      pattern: "parseMentions"
---

<objective>
Build the MentionTextarea shared component and integrate it into QATab, VideoWatchView, and ArticleEditor. Add YouTube attachment support to Q&A posts. Add @mention rendering in displayed comments/posts and article text blocks.

Purpose: Enable @mentions across all text input surfaces (video comments, Q&A posts/replies, and article text blocks per locked decision) and add YouTube attachment to Q&A — the two primary user-facing features that feed into notifications.
Output: New MentionTextarea component, updated QATab with YouTube attachment, updated VideoWatchView comment composer, updated ArticleEditor text blocks, updated ArticleViewer mention rendering, mention rendering in displayed text.
</objective>

<execution_context>
@/Users/jakeweinstein/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakeweinstein/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engagement/04-CONTEXT.md
@.planning/phases/04-engagement/04-RESEARCH.md
@.planning/phases/04-engagement/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From lib/mention-utils.ts:
```typescript
export function parseMentions(text: string): Array<{ type: 'text' | 'mention'; value: string }>
```

From lib/comment-utils.ts:
```typescript
export function timeAgo(dateStr: string): string
export function initials(name: string): string
export function avatarColor(name: string): string
```

From lib/types.ts:
```typescript
export type Comment = {
  id: string; video_id?: string; session_id?: string; timestamp_seconds?: number;
  comment_text: string; author_name: string; author_id?: string;
  send_to_captain: boolean; parent_id?: string; created_at: string;
  is_edited?: boolean; updated_at?: string; youtube_attachment?: string;
  reply_count?: number;
}
export function extractYouTubeInfo(input: string): { id: string; startSeconds?: number } | null
```

From components/QATab.tsx:
```typescript
interface QATabProps { userName: string }
// State: posts, commentText, sendToCaptain, replyingTo, replyText
// handlePost() POSTs to /api/comments
```

From components/VideoWatchView.tsx:
```typescript
// Comment composer uses textarea for comment_text input
// Has userId, userName, userRole props
// playerRef.current.getCurrentTime() available
```

From components/ArticleEditor.tsx:
```typescript
// TextBlock sub-component uses <textarea> for block.content editing
// Has auto-resize via taRef and handleChange
// Props flow: ArticleEditor -> TextBlock(i, block, updateBlock)
```

From components/ArticleViewer.tsx:
```typescript
// Text blocks rendered via: <ReactMarkdown>{block.content}</ReactMarkdown>
// Inside prose prose-sm prose-gray container
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MentionTextarea component + mention rendering utility</name>
  <files>components/MentionTextarea.tsx, lib/comment-utils.ts</files>
  <action>
1. Create `components/MentionTextarea.tsx` ('use client'):
   - Props: { value: string, onChange: (val: string) => void, users: Array<{ id: string, username: string, displayName: string }>, placeholder?: string, rows?: number, className?: string, onKeyDown?: (e: React.KeyboardEvent) => void, autoResize?: boolean }
   - Internal state: mentionSearch (string | null), caretPos (number), selectedIndex (number, for keyboard nav)
   - Ref to the textarea element for caret position tracking
   - handleChange: on every change, read selectionStart, look backward from caret for /@(\w*)$/ pattern. If found, set mentionSearch to the captured group. Otherwise set mentionSearch to null.
   - Filtered users: when mentionSearch is not null, filter users where username starts with mentionSearch (case-insensitive) OR displayName includes mentionSearch (case-insensitive), limit to 8 results.
   - selectUser(user): replace the @partial before caret with `@${user.username} `, call onChange with updated text, set mentionSearch to null, refocus textarea.
   - Keyboard handling: when dropdown is open, ArrowDown increments selectedIndex (wrap), ArrowUp decrements (wrap), Enter/Tab calls selectUser with selected item and prevents default, Escape closes dropdown. When dropdown is NOT open, pass through to props.onKeyDown if provided.
   - If autoResize prop is true, auto-resize the textarea height on content change (same pattern as ArticleEditor's TextBlock: set height to 'auto' then scrollHeight + 'px').
   - Render: relative container wrapping a <textarea> with the passed className/rows/placeholder. When filtered users has items, render an absolute-positioned dropdown (bottom of textarea, z-50, bg-white shadow-lg border rounded-lg max-h-48 overflow-y-auto) listing each user with displayName and @username in muted text. Highlight selectedIndex item with bg-blue-50. Click on item calls selectUser.
   - The textarea gets all standard attributes (value, onChange, placeholder, rows, className).
   - Expose the textarea ref via React.forwardRef so parents can access it if needed (ArticleEditor needs ref for auto-resize).

2. Update `lib/comment-utils.ts`:
   - Add import of parseMentions from '@/lib/mention-utils'
   - Add and export renderMentionSegments function that takes text and returns React elements: map parseMentions result, 'mention' segments become <strong className="text-blue-600 font-semibold">{value}</strong>, 'text' segments are plain text. Import React for createElement. Return array of ReactNode.
   - Alternatively, export a simpler approach: just re-export parseMentions from mention-utils so components can use it directly for rendering. The rendering pattern (map + strong for mentions) is simple enough to inline in each component. Choose the re-export approach to avoid importing React into a utility file.
   - Add: export { parseMentions } from '@/lib/mention-utils'
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>MentionTextarea renders a textarea with @mention autocomplete dropdown. Keyboard navigation (arrows, enter, tab, escape) works. parseMentions re-exported from comment-utils for rendering convenience.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate MentionTextarea into QATab + VideoWatchView + ArticleEditor + ArticleViewer</name>
  <files>components/QATab.tsx, components/VideoWatchView.tsx, components/ArticleEditor.tsx, components/ArticleViewer.tsx</files>
  <action>
1. Update `components/QATab.tsx`:
   - Add state: users (MentionUser[]), fetched once on mount via GET /api/users (the existing endpoint returns user list — may need to handle auth; if unauthenticated, fetch returns empty and mentions won't work for anon users, which is fine since posting requires auth).
   - Actually, QATab already requires auth (userName prop). Fetch users on mount: GET /api/users — this is captain-only in current implementation. Instead, create a lightweight approach: add a new query param to an existing route OR use the users visible from comment authors. Better approach per research: fetch GET /api/users with auth cookie — if it returns 403 (non-captain), fall back to empty list. But this is suboptimal. The cleanest solution: add a public users endpoint for display_name + username only (no sensitive data). Add GET /api/users/list route that returns [{id, username, display_name}] for any authenticated user. WAIT — this touches a new file not in this plan. Instead, modify the existing /api/users GET to allow any authenticated user to see the basic user list (id, username, display_name only) — captain still gets full details. This change is in app/api/users/route.ts which is NOT in this plan's files_modified. So: fetch from /api/users and if 403, use empty users array. The mention feature degrades gracefully — no dropdown but @username still parsed server-side.
   - REVISED APPROACH: Add a users fetch to QATab that calls GET /api/users. The existing route returns 403 for non-captain. To avoid modifying that route in this plan, pass users as a prop to QATab: QATabProps gets optional users?: MentionUser[]. The parent (app/page.tsx or wherever QATab is rendered) fetches users if authenticated. If users prop is empty/undefined, MentionTextarea dropdown simply never shows.
   - SIMPLEST APPROACH (per research recommendation): Fetch user list once at the page level in app/page.tsx when authUser is set, pass it down. QATab and VideoWatchView both receive users prop. This means modifying app/page.tsx — but that file is in Plan 03. So for THIS plan, accept users as a prop.
   - Update QATabProps: add users?: Array<{ id: string, username: string, displayName: string }>
   - Replace the post composer <textarea> with <MentionTextarea> passing value={commentText}, onChange={setCommentText}, users={users ?? []}.
   - Replace the reply <textarea> with <MentionTextarea> passing value={replyText}, onChange={setReplyText}, users={users ?? []}, rows={2}.
   - Add YouTube attachment field below the post composer textarea:
     - State: attachmentUrl (string), initialized to ''
     - Input field: <input type="text" placeholder="Paste YouTube URL (optional)" value={attachmentUrl} onChange={...} className="..."/>
     - Parse: const parsedAttachment = attachmentUrl.trim() ? extractYouTubeInfo(attachmentUrl.trim()) : null
     - If parsedAttachment is not null, show a preview embed: <div className="rounded-lg overflow-hidden aspect-video max-w-sm"><iframe src={`https://www.youtube.com/embed/${parsedAttachment.id}`} className="w-full h-full" allowFullScreen /></div>
     - If attachmentUrl is non-empty but parsedAttachment is null, show red text "Invalid YouTube URL"
     - In handlePost(): add youtube_attachment: parsedAttachment?.id ?? undefined to the POST body
     - Clear attachmentUrl after successful post
   - Remove the sendToCaptain checkbox from Q&A posts — per locked decision, Q&A posts are always captain-facing. The send_to_captain field is forced true server-side (Plan 01). Remove the sendToCaptain state and checkbox UI.
   - When rendering posts and replies, render comment_text with @mention highlighting: use parseMentions (re-exported from comment-utils) to split text, map mention segments to <strong className="text-blue-600 font-semibold">{value}</strong>.
   - When rendering posts that have youtube_attachment field set, show an embedded YouTube iframe below the post text: <div className="mt-2 rounded-lg overflow-hidden aspect-video"><iframe src={`https://www.youtube.com/embed/${post.youtube_attachment}`} className="w-full h-full" allowFullScreen /></div>

2. Update `components/VideoWatchView.tsx`:
   - Add users prop to VideoWatchViewProps: users?: Array<{ id: string, username: string, displayName: string }>
   - Replace the comment composer <textarea> with <MentionTextarea> passing the same value/onChange props, users={users ?? []}.
   - When rendering comment text in the comment list, use parseMentions to split and render @mentions as bold blue text (same pattern as QATab).

3. Update `components/ArticleEditor.tsx` (per locked decision: @mentions work in article text blocks):
   - Import MentionTextarea from './MentionTextarea'.
   - The ArticleEditor is rendered inside DashboardView which has userId/userName. ArticleEditor needs a users prop for the mention dropdown.
   - Add optional users prop to ArticleEditor: users?: Array<{ id: string, username: string, displayName: string }>
   - In the TextBlock sub-component, replace the plain <textarea> with <MentionTextarea>. Pass: value={block.content}, onChange={(val) => updateBlock(i, { content: val })}, users={users ?? []}, autoResize={true}, className (same classes as the current textarea), placeholder="Write your content... Use @ to mention team members".
   - The auto-resize behavior currently in TextBlock's handleChange should be handled by MentionTextarea's autoResize prop (added in Task 1). Remove the manual auto-resize logic from TextBlock since MentionTextarea handles it.
   - Per RESEARCH.md recommendation: skip notification insertion for article context — just wire the component for input. Server-side @mention parsing in articles is NOT needed (articles are published content, not direct replies). The @username text will be stored as-is in block.content.

4. Update `components/ArticleViewer.tsx` (render @mentions as bold blue in article text blocks):
   - Import parseMentions from '@/lib/mention-utils'.
   - For text blocks: before passing content to ReactMarkdown, pre-process @mentions. Since ReactMarkdown renders markdown, and @mentions should appear as bold blue: replace @username patterns in the markdown source with **<span style="color:#2563eb">@username</span>** — but this is fragile with markdown.
   - BETTER APPROACH: After ReactMarkdown renders, @mentions won't be styled. Instead, for text blocks that contain @mentions (check with /@\w+/ regex), render a custom component: split content by lines, for each line use parseMentions to identify mention segments, render mentions as bold blue spans and text segments via ReactMarkdown (inline mode) or as plain text. This is complex.
   - SIMPLEST APPROACH: Use ReactMarkdown's components prop to post-process text nodes. Or simply: if block.content contains @mentions, render using parseMentions mapping instead of ReactMarkdown. If no @mentions detected, use ReactMarkdown as before. This provides correct mention styling while preserving markdown rendering for non-mention content.
   - PRAGMATIC APPROACH: Use a custom ReactMarkdown component override for the `text` node type. In the components prop, override `text` (or `p` children) to scan for @mention patterns and wrap them in bold blue spans. Specifically: pass components={{ p: ({children}) => <p>{processChildren(children)}</p> }} where processChildren walks ReactNode children, finds string nodes, splits by @mention regex, and wraps mentions in <strong className="text-blue-600 font-semibold">. Non-string children pass through unchanged.
   - This ensures markdown formatting (bold, italic, links, lists) still works AND @mentions get styled.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>QATab has MentionTextarea for posts and replies, YouTube URL input with preview and validation, mention rendering in displayed posts. VideoWatchView has MentionTextarea for comment composer and mention rendering in comment display. ArticleEditor text blocks use MentionTextarea for @mention autocomplete. ArticleViewer renders @mentions as bold blue text in article text blocks. All components accept users prop for autocomplete.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx vitest run --reporter=verbose` passes (existing tests still green)
- MentionTextarea component renders with textarea and conditional dropdown
</verification>

<success_criteria>
- MentionTextarea is a reusable component with @ autocomplete, keyboard navigation, and user filtering
- QATab uses MentionTextarea for post and reply composers
- QATab has YouTube URL input with extractYouTubeInfo validation and embed preview
- QATab no longer shows send_to_captain checkbox (server-side forced)
- QATab renders youtube_attachment embeds on displayed posts
- VideoWatchView uses MentionTextarea for comment composer
- ArticleEditor text blocks use MentionTextarea with autoResize for @mention autocomplete
- ArticleViewer renders @mentions as bold blue text within markdown content
- @mentions render as bold blue text in all displayed comments, Q&A posts, and article text blocks
- All components accept users prop for autocomplete data
</success_criteria>

<output>
After completion, create `.planning/phases/04-engagement/04-02-SUMMARY.md`
</output>
